import ContactView from '@/components/content/ContactView.jsx';

export const metadata = {
  title: 'Contact us',
  description: 'Questions about your delivery, our farms, or what’s fresh? Talk to the Farm to Table team.'
};

/** `/contact` */
export default function ContactPage() {
  return <ContactView />;
}
